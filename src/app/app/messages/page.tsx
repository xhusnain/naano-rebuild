import { Stub } from "@/components/app/Stub";

export default function MessagesPage() {
  return (
    <Stub
      title="Messages"
      why="Naano has creator↔brand messaging. In a 24-hour build it is a large surface — threads, read state, notifications, realtime — that demonstrates none of what makes this product distinctive, because it is the same chat you have seen everywhere else."
      instead="The attribution loop: per-creator tracked links, live click counts, and the deal lifecycle that moves a booking from invited to paid."
    />
  );
}
